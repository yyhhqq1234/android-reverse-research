# NECR 准备工作状态（只读原包，所有产出在 work_necr）

原包：`D:\安卓逆向\NECR\Necromancer.apk`（SHA256 91F35185…896024，未动）
解包：`D:\安卓逆向\NECR\Necromancer\`（未动）

## 已完成（我做的）

- [x] 工具链：`tools\apktool.jar`(3.0.3, 已验版本) / `tools\il2cppdumper\`(6.7.46, 已试跑成功)
      / `tools\ASModGUI.zip`(AssetStudioMod v0.19.0, net472免装版, 已验包完整)
      / `tools\BlackDex32.apk`(v3.2) / `tools\frida-server-x86_64.xz`+`frida-server-x86.xz`(17.18.0, 与本机frida-tools 14.10.4配套)
- [x] 本机 frida-tools 14.10.4 + frida-py 17.18.0（pip 已装；CLI 不在 PATH，用 `python -m frida` 或补 PATH）
- [x] `tools\g1_gate.py`：G1 校验门，已用原包三件套自检 ALL PASS
      用法：`python tools\g1_gate.py <dump产物文件或目录>`
- [x] `tools\anti_ptrace.js`：B 级 Frida 反检测骨架（ptrace 放行 + maps 观察 + prop 观察 + RegisterNatives 记录，只记录不改行为）
- [x] **Il2CppDumper 试跑成功**：用原包明文 `libil2cpp.so + global-metadata.dat` 产出
      `trial\out_vanilla\`（dump.cs 12MB/30万行含 MainManager/RebirthLevelPlus/AddToSoltRebirth、
      il2cpp.h、script.json、stringliteral.json、DummyDll）——后链工具先行验证通过
- [x] 目录：`work_necr\{tools,prod,logs,trial}`；雷电现有 1 实例（index 0，未启动，Android 版本待开机确认）

## 待你动手（我做不了的）

1. 启动雷电实例 index 0，开机后告诉我一声（我来 `adb` 接管后面所有命令行活）。
   开机后请顺手确认：设置里 Android 大版本（期望 9；若是 7 也可用，对应 32 位对照）。
2. 按 T3 净化清单做机型伪装（品牌/型号/IMEI 改真机值，关代理），先跑 RootBeer 自查（我可提供 APK 链接）。
3. 在模拟器里装原版 NECR（`adb install` 我可代劳），**手动点进主界面**（解密发生的标志）。
4. 装 GG 修改器（gameguardian.net，需注册下载；位数跟目标一致用 32 位思路），下载
   `Dex Dump.lua`（gameguardian.net/topic/31915；MT 论坛 tid=152033 镜像）与
   `Il2CppGG`（GitHub lethi9gg）——这两个论坛附件要登录，我下不了。
5. 按 A 级步骤执行 GG 脚本 dump，把 `/sdcard/Download` 与 `/sdcard/GGDump` pull 回来丢进
   `work_necr\prod\` 并告诉我——后面 G1 门、Il2CppDumper 对照、后链全归我。

## 版本钉死（勿动）

frida PC 17.18.0 == frida-server 17.18.0；Il2CppDumper 6.7.46；apktool 3.0.3；
AssetStudioMod v0.19.0；BlackDex v3.2；GG 版本装好后记 md5。
