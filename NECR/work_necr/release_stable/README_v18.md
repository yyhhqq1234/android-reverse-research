# v18 — 购买闪退根治版 (2026-09-12)

成品: `necr_menu_v18.apk` (50923268 B, 同密钥覆盖安装, play 测试通过)

## 根因 (v17s  verdict)
- 菜单 P-gate (`0x1AABE2C`, "Max plus" idx2) + idx0-gate ("G grade") 的函数体
  一旦在购买路径上执行就 smash 返回地址: gate#1 返回后永远到不了 `0x998CB0`
  (A06 整数日志 `G1=` 全程静默, POST B03 之后 ~10ms 以 pc=0/SI_USER 崩溃)。
- 反证: 7 处调用点恢复原版直调 (`0x998CAC/D0C/D40/D74/DA8 + 0x999E20 ->
  BL 0x12896E0`, `0x999E74 -> MOV R0,R5`) 后购买一次通过:
  `CALL a1=49 -> G1=19376 -> RET`, 单位落格 6/42, 钻石 -10, 无崩溃。
- 排查史: G-GUARD / gate-OFF 跳板调用 / B01 / B02门 / 读旗文件IO /
  B03洞帧逐一排除 (v17k/v17l/v17m/v17n/v17o/v17p/v17q/v17r 全灭);
  安装新鲜度经 v17q 优先级标记 (`W N17E`) 独立验证, 管道可信。

## v18 内容 (`tools/restore_v18.py`)
- 保留修复: 上述 7 处原版直调 + B03 内调用原版化 + E/M 原版。
- 恢复菜单功能: G-GUARD (`0x12185B4 -> 0xEB248D29`, 免费抽卡)。
- 剥离全部 trace/debug 钩子 (outer/A04/A05/B01/B02/B03/B05/B06/N17Dx4/B2/A06
  共 17 字恢复原版; B01/B02 洞内调用、读旗头、A04 优先级标记同步还原)。
- 代价: "Max plus"(idx2) 与 "G grade"(idx0) 在购买路径上不再生效
  (单位按自然数值/品阶掉落; 这两个开关原本在基线配置就是 OFF)。
  徽章显示恢复原版 (A+++ 回归, guards 剥离的副作用自愈)。

## 验证矩阵 (MuMu 12 / Android 12, `127.0.0.1:16384`)
- 冷启动干净 (测试配置 `00000010000` + 基线 `00000000011`)。
- 购买成功: 测试配置免费抽卡生效 (钻石不扣); 基线按原价扣钻; 单位落格。
- 背包徽章: A+++ 正常显示 (与原版一致)。
- 角色面板 / 技能书 / MixBox 面板打开无闪退。
- 传送门进战斗: 本轮未走通导航 (角色走到门边但未触发进入; 非崩溃,
  游戏全程存活)。战斗路径无菜单补丁, v18 等同原版 + 菜单静态区。
- 存档健康: 仅多 1 个测试购买单位 (Inventory5=49, -10钻), 无 corruption;
  全量备份 `save/necr_save_bak_20260912.tgz` (01:32)。

## 回滚
- SO 备份链: `src/lib/armeabi-v7a/libil2cpp.so.bak-*` (每步一备);
  v18 前快照 `libil2cpp.so.bak-pre-v18`; 用户可玩基线 v17h 包仍在
  `release_v17h/`。
